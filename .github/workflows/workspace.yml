name: Workspace

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    uses: langri-sha/github/.github/workflows/check.yml@v0.15.1
    with:
      dagger: true
      eslint: true
      prettier: true
      projen: true
      vitest: true
    secrets: inherit
