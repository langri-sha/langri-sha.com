name: Terraform

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/terraform.yml'
      - '*.tf'
      - '*.lock.hcl'
  pull_request:
    paths:
      - '.github/workflows/terraform.yml'
      - '*.tf'
      - '*.lock.hcl'

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - run: echo "OK"
