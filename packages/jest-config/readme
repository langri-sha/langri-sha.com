# @langri-sha/jest-config

Basic, reusable [Jest] configuration.

## Usage

Install required dependencies:

```sh
npm install -D jest
```

```js
// jest.config.js
import config from '@langri-sha/jest-config'

export default {
  ...config,
  /* ... */
}
```

[jest]: https://jestjs.io/docs/
